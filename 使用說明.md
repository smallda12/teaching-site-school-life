# 上學真有趣｜教學網　使用說明

語文　115 學年度 第 1 學期　第 1 單元　第 1～4 週
（115 學年度第一學習階段新班級的第一個單元）

## 這是什麼

把〈上學真有趣〉整個單元的教材做成一個**純靜態網站**，雙擊 `index.html` 就能用，
不必安裝、不必連網（只有影片與 PDF 是本機檔案，一樣離線可用）。

## 兩種開啟方式

| 開法 | 版本 | 差別 |
|---|---|---|
| 雙擊本資料夾的 `index.html` | **教室版** | 點名區可以上傳學生照片 |
| 用線上網址開啟 | **公開版** | 照片上傳自動關閉，只能輸入姓名 |

學生姓名存在瀏覽器的 localStorage、照片存 IndexedDB，**只留在那台電腦，不會上傳到網路，也不在 GitHub 裡**。

## 七個分區

| 分區 | 內容 |
|---|---|
| 🙋 點點名 | 抽籤點名，一輪每人只點一次，名單可匯出匯入 |
| 🃏 圖詞輪播 | 9 張學習圖卡，詞與說明各自可單獨發音 |
| 🎬 教學影片 | 主影片 1 支＋主題短片 3 支，右邊清單可自由切換 |
| 📚 教材教學 | 三個重點（上學的好處／學校裡的活動／選出我最喜歡的活動），每個重點教學頁後接 5 題形成性評量 |
| 📝 評量區 | 15 題集中做一次，做完顯示各重點正確率；另可開學習單、評量練習單、學習圖卡與完整簡報 PDF |
| 🧠 自學區 | 圖卡牆，點一下放大，可自動輪播 |
| 🔗 延伸學習 | 3 支外部教學影片（巧虎TV、慧珊老師、花媽親子淘），點了開新分頁到 YouTube，**這一區需要網路** |

側邊還有 🖊️ 畫筆與 ⏱️ 三組獨立計時器。

## 教材內容

- 頁圖 **62 頁**（教材 PPTX、PDF、紙本都由這 62 張產生）
- 三個重點：上學的好處（P4–6＋P42–43）／學校裡的活動（P15–17＋P46–47）／選出我最喜歡的活動（P26–28＋P50–51）
- 評量共 **15 題**兩選一（每重點 4 題正式＋1 題深化），呈現順序 `BBAABBAABBAABBA`（A 7 題／B 8 題）
- 學習圖卡 **9 張**（每個重點各 3 張）
- 語音 **77 支**（`zh-TW-HsiaoChenNeural`，語速 -25%）
- 影片 **4 支**：教學影片（字幕版）、上學的好處、學校裡的活動、選出我最喜歡的活動
- 延伸學習外部影片 **3 支**（老師 2026-09-10 確認的片單，全部掛在重點一「上學的好處」）

## 要更新內容時

**`語文_第一學期_第1單元_上學真有趣\pages\` 才是教材本體**，其餘（PPTX、PDF、紙本、影片、本網站）全是衍生檔。

```
1. python 重生頁圖.py p27              # 只重生要改的那一張（若有這支腳本）
2. 逐字校對 → 縮放成頁圖尺寸 → 覆蓋 pages\p27.png
3. 重跑下游：重出簡報 → 重建紙本 →（影片有引用到才）重建教學影片.py
4. python 內容稽核.py <單元> --full   # ③④⑦ 要逐張讀圖
5. 網站：工具腳本\建置素材_圖片壓縮.py → 工具腳本\產出網站資料.py →（改到題目或口播才要）建置語音_全部.py
6. 跳版本號 → git commit → git push
```

🛑 **改了 `教學網站\` 裡的任何東西，`index.html` 的 `?v=` 就要加 1**。
`資源版本` 與 `語音.版本` 都住在 `config.js` 裡，而 `config.js` 本身也是用 `?v=` 載入的——
只改它們而不跳 `index.html` 的 `?v=`，回訪者會拿到快取裡的舊 `config.js`，等於沒更新。

## 題目的唯一來源

題目、選項與正解的唯一來源是素材資料夾的 `題庫.py`。
網站的 `js/data.js` 與語音腳本用的 `工具腳本\語音內容.py`（都由 `產出網站資料.py` 同一次產生）
一定要跟它一致，改完題庫後在單元教材資料夾（`pages\` 那一層）跑：

```
python 工具腳本/產出網站資料.py
```
